<!--- Vat types --->
<div class='caja_completa'>
  <%= index_header 'vats', {url: new_admin_vat_path, title: 'Crear nuevo tipo de IVA' } %>
    <% for vat in @vats %>
      <%= render partial: 'vat', locals: {vat: vat} %>
    <% end %>
  <%= index_footer %>
</div>