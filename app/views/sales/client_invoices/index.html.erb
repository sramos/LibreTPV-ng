<!--- Invoices --->
<div id="invoices">
  <%= index_header 'client_invoices' %>
    <% for invoice in @invoices %>
      <%= render partial: 'item', locals: {invoice: invoice} %>
    <% end %>
  <%= index_footer @invoices %>
</div>