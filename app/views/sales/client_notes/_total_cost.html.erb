<div id="importe_total">
  <div class="importe_grande">
    Total<br/><%= format('%0.2f',@note.total_amount) %> €
  </div>
  <div class='elemento_derecha'>
    <% unless @note.closed %>
      <% disabled = @note.total_amount.zero? %>
      <%= object_edition icon: icon('euro', size: '2x', title: 'Efectuar cobro'),
                         url: new_sales_client_note_invoice_path(@note.id),
                         disabled: disabled %>
    <% end %>
    <br/>
    <% confirmation_text = "¿Estás seguro de eliminar la cesta?" %>
    <%= object_destroy icon: icon('trash-can', size: '2x', title: 'Eliminar cesta'),
                       url: sales_client_note_path(@note.id, redirect: 1),
                       confirmation: confirmation_text,
                       disable_turbo_frame: true %>
  </div>
</div>
